
typedef u32 tunnel_unit;
typedef u64 tunnel_key;

typedef enum tunnel_mode tunnel_mode_t;
typedef enum tunnel_state tunnel_state_t;

typedef struct if_tunnel_s if_tunnel_t;

struct tunnel_ctx;
struct tunnel_domain;
struct tunnel_stats;
